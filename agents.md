
# Workspace Instructions

When the user sends a message starting with a `/` command, read `origin.md` and follow the **Commands** section to route and execute the command.

To find the list of recognized commands, read the `## Commands` section in `origin.md` — each `### /command` heading is a valid command.
